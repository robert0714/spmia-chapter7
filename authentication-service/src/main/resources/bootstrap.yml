spring:
  application:
    name: authenticationservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
management:
  security:
     enabled: false

security:
  oauth2:
    resource:
      filter-order: 3